import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Page = () => {
    return (
        <View style={styles.container}>
            <Text>Bird Identification Preview</Text>
        </View>
    );
};

const styles = StyleSheet.create({
    container: {},
});

export default Page;
